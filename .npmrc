registry=https://registry.npmjs.org/
# Auth token supplied via NODE_AUTH_TOKEN in workflow; avoid embedding secrets here.
# Uncomment below if publishing also to GitHub Packages and set NODE_AUTH_TOKEN accordingly.
# @sylvesterllc:registry=https://npm.pkg.github.com
# always-auth=true